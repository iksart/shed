import { classNameWrapper } from '../../wrapper';
import { TableTreeColumn } from '@devexpress/dx-react-grid-bootstrap4';

export const TaskTreeExpandButtonComponent = classNameWrapper(TableTreeColumn.ExpandButton, { addClassName: 'line-height_tight-very' });